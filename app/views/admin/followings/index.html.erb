
<div class="container">
  <!--ヘッダー-->
  <%= render partial: "admin/shared/header" %>

  <div class="row">
    <div class="col-lg-6">
      <h2><%= "#{@user.name}がフォローしているユーザ：#{@followings.count}"%></h2>
    </div>
  </div>
  <br>

  <div class="row">
    <div class="col-lg-6">
    <% if @followings.present? %>
      <!--ユーザ（フォロー）一覧-->
      <%= render "admin/users/index_users", users: @followings %>
      <%= paginate @followings %>
    <%else%>
      -フォローしているユーザはいません-
    <%end%>
    </div>
  </div>

</div>