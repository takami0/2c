
<div class="container">
  <h1>ユーザ(一覧)</h1>
  <%= render partial: "admin/shared/header" %>

  <%= render partial: "admin/shared/index_users", locals: {users: @users} %>
  <%= paginate @users %>

</div>
