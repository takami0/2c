
<h4>-<投稿検索>-</h4>
<table class= "table-bordered">

  <!--検索対象：媒体-->
  <%= form_with url: public_search_path, method: :get do |f| %>
  <tr>
    <th>媒体：</th>
    <td><%= f.collection_select :category_medium_id, CategoryMedium.all, :id, :name, prompt: "制作媒体を選択ください" %></td>
    <td>
      <%= f.search_field :word %>
      <%= f.hidden_field :subject, :value => "投稿_媒体別" %>
      <%= f.submit "検索" %>
    </td>
  </tr>
  <%end%>

  <!--検索対象：モチーフ-->
  <%= form_with url: public_search_path, method: :get do |f| %>
  <tr>
    <th>モチーフ：</th>
    <td><%= f.collection_select :category_motif_id, CategoryMotif.all, :id, :name, prompt: "制作モチーフを選択ください" %></td>
    <td>
      <%= f.search_field :word %>
      <%= f.hidden_field :subject, :value => "投稿_モチーフ別" %>
      <%= f.submit "検索" %>
    </td>
  </tr>
  <%end%>

  <!--検索対象：スタイル-->
  <%= form_with url: public_search_path, method: :get do |f| %>
  <tr>
    <th>スタイル：</th>
    <td><%= f.collection_select :category_style_id, CategoryStyle.all, :id, :name, prompt: "制作スタイルを選択ください" %></td>
    <td>
      <%= f.search_field :word %>
      <%= f.hidden_field :subject, :value => "投稿_スタイル別" %>
      <%= f.submit "検索" %>
    </td>
  </tr>
  <%end%>

</table>